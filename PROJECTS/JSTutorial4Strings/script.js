//toUpperCase() - turn string to uppoercase
//toLoerCase() - turn string to lowercase
//trim() = removes extra spaces on start to end of a string 
//trimEnd() - remove extra spaces in the end of a string
//trimStart() remove extra spaces in the start of a string

//replace() - replaces first matching word on a string
//replaceAll() - replaces all matching words on a string
//slice() gets a part of a string by specifying the start and end index
let word = `Dyames`;

alert(word.length)